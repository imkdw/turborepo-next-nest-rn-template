# Test Database (uses separate port/db for isolation)
DATABASE_URL=postgresql://postgres:postgres@localhost:7432/turborepo-template-postgres-test?schema=public

# API Server
API_PORT=8001
APP_ENV=test

# Swagger Authentication
SWAGGER_USERNAME=test
SWAGGER_PASSWORD=test
